spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/vetbuddy
    username: vetbuddy
    password: vetbuddy
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false

  liquibase:
    change-log: classpath:db/changelog/db.changelog-master.yaml
